{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug the API",
      "program": "${workspaceFolder}/dist/server.js",
      "preLaunchTask": "npm: build"
    }
  ]
}
